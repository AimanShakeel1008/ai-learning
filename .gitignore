# Local-only course control files - never commit these
CLAUDE.md
curriculum.md
progress-tracker.md

# Jupyter / Python noise
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/

# Local Claude Code settings - machine-specific, never commit
.claude/

# Real secrets - never commit; customer-support-assistant/.env.example is the
# committed template, this is where the learner's actual keys live
customer-support-assistant/.env
